{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build && cd artifacts/sandipani && node ./node_modules/vite/bin/vite.js build && cd ../api-server && node ./build.mjs && cd ../mockup-sandbox && BASE_PATH=/ node ./node_modules/vite/bin/vite.js build",
    "typecheck:libs": "tsc --build",
    "typecheck": "tsc --build"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
